<!DOCTYPE html>
<html>
<head>
  <title>Bart-Live | Interactive Bart Resource </title>
  	  <link rel="shortcut icon" href="/assets/bart_live_icon.ico" />
	  <link rel="image_src" href="/assets/facebook_image_alt.jpg" />
	  <meta name="description" content="Bart-Live is a mobile-friendly application for Bart commuters designed to elimate the need for waiting at the station.  View departures in real-time, find the closest station, set station-specific text reminders and more! This app works best with Chrome." />
  	  <meta name="keywords" content="Bart, BART, Bay Area Rapid Transit, travel, commute, traffic, train, trains, departure, arrival, eta, edt, time, times, live, mobile, web, app, application, station, stations">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  
  <script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-44536817-1', 'bart-live.com');
    ga('send', 'pageview');

  </script>
</head>
<body>
	<div id="wrap">
		<div id="navbar_container">
			<%= render "shared/navbar" %>
		</div>
		<% flash.each do |key, val| %>
			<% if key == :success %>
				<div class="alert alert-success"> 
					<button type="button" class="close" data-dismiss="alert">×</button>
					<%= val.join(", ") %> 
				</div>
			<% else %>
				<div class="alert alert-error"> 
					<button type="button" class="close" data-dismiss="alert">×</button>
					<%= val.join(", ") %> 
				</div>
			<% end %>
		<% end %>
		
		<%= yield %>
    </div>
	<%= render "shared/footer" %>
</body>
</html>
