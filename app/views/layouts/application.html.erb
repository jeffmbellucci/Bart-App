<!DOCTYPE html>
<html>
<head>
  <title>Bart-Live | Interactive Bart Resource </title>
  	  <link rel="shortcut icon" href="/assets/bart_live_icon.ico" />
	  <link rel="image_src" href="/assets/facebook_image.jpg" />
	  <meta name="description" content="Bart-Live is a mobile-friendly resource for Bart commuters designed to elimate the need for waiting at the station.  View departures in real-time, find the closest station, set station-specific text reminders and more!" />
  	  <meta name="keywords" content="Bart, BART, Bay Area Rapid Transit, travel, commute, traffic, train, trains, departure, arrival, times, live, mobile, web app, stations">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div id="wrap">
		<div id="navbar_container">
			<%= render "shared/navbar" %>
		</div>
		<% flash.each do |key, val| %>
			<% if key == :success %>
				<div class="alert alert-success"> 
					<button type="button" class="close" data-dismiss="alert">×</button>
					<%= val.join(", ") %> 
				</div>
			<% else %>
				<div class="alert alert-error"> 
					<button type="button" class="close" data-dismiss="alert">×</button>
					<%= val.join(", ") %> 
				</div>
			<% end %>
		<% end %>
		
		<%= yield %>
    </div>
	<%= render "shared/footer" %>
</body>
</html>
